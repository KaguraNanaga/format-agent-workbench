# 隐私说明

Format Agent Workbench 在本机运行。你上传的文档先保存到本机临时目录，生成的结果也保存在本机；项目不会收集遥测数据。

真实任务需要调用你在 `.env` 中配置的模型服务。为了提取规则、识别文档结构与执行可选视觉自检，以下内容可能发送给该服务：

- 你输入的格式要求；
- 文档中用于结构识别的文字；
- 开启视觉自检时，文档页面的渲染图片。

数据的保存与使用方式取决于你选择的模型服务商，请在处理敏感文档前阅读其隐私条款。

## 默认保护措施

- `.env`、运行时目录和输出目录已加入 `.gitignore`。
- `LLM_ALLOW_PUBLIC_IMAGE_UPLOAD=false` 默认启用，页面图片以内联方式发送给模型服务，不经过公共图床。
- Windows 启动脚本只监听 `127.0.0.1`，不会主动向局域网公开工作台。
- 内置演示使用仓库内的示例文件和固定规则，不调用外部模型。

## 使用建议

- 不要把 API Key、真实文档或输出文件提交到 GitHub。
- 处理机密材料前，确认模型服务商允许相应数据类型，并优先使用符合组织要求的私有部署。
- 如果不需要页面级检查，可以关闭视觉自检，减少发送给模型的内容。
